# BrandVault - 개발/운영 인프라 기술서

## 문서 정보
- **프로젝트명**: BrandVault
- **작성일**: 2025-11-29
- **배포 URL**: https://brand-vault.vercel.app
- **문서 목적**: 최종 평가를 위한 개발/운영 인프라 상세 기술 보고서

---

## 1. 프로젝트 개요

### 1.1 시스템 아키텍처
BrandVault는 3D 제품 자산 관리 및 AI 기반 비디오 편집 기능을 제공하는 웹 애플리케이션으로, 클라이언트-서버 분리 아키텍처로 구현되었습니다.

**주요 구성 요소**:
- Frontend: React 기반 SPA (Single Page Application)
- Backend: Node.js + Express 기반 RESTful API 서버
- Database: PostgreSQL (Prisma ORM 사용)
- Storage: Google Cloud Storage (파일 저장소)
- AI Services: Replicate API (3D 변환 및 비디오 편집)

**핵심 기능**:
1. **3D 자산 변환**: 2D 이미지를 3D Gaussian Splat 모델로 변환 (Replicate Trellis 모델 사용)
2. **비디오 제품 삽입**: AI 기반 비디오 내 제품 삽입 (SAM2, Anydoor, AnyV2V 모델 활용)
3. **다중 사용자 역할**: Creator, Brand, Advertiser 3가지 역할 지원
4. **제품 마켓플레이스**: 3D 제품 업로드/다운로드/구매
5. **실시간 작업 진행 상태**: WebSocket 기반 실시간 진행률 표시

---

## 2. Frontend 개발 환경

### 2.1 런타임 및 빌드 시스템

| 항목 | 버전 | 역할 |
|------|------|------|
| Node.js | v22.21.1 | JavaScript 런타임 환경 |
| npm | v10.9.4 | 패키지 관리자 |
| React Scripts | 5.0.1 | Create React App 기반 빌드 시스템 (Webpack, Babel 포함) |

### 2.2 핵심 프레임워크

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| **React** | 19.2.0 | UI 라이브러리 |
| **React DOM** | 19.2.0 | 브라우저 DOM 렌더링 |
| **TypeScript** | 4.9.5 | 정적 타입 검사 |
| **React Router DOM** | 7.9.4 | 클라이언트 사이드 라우팅 |

**TypeScript 컴파일러 설정**:
```typescript
{
  "target": "es5",           // IE11 호환
  "module": "esnext",        // 최신 모듈 시스템
  "jsx": "react-jsx",        // React 17+ JSX 트랜스폼
  "strict": true,            // 엄격한 타입 검사
  "moduleResolution": "node" // Node.js 모듈 해석
}
```

### 2.3 3D 렌더링 스택

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| **Three.js** | 0.181.0 | WebGL 기반 3D 렌더링 엔진 |
| **@react-three/fiber** | 9.4.0 | React용 Three.js 래퍼 (선언적 3D 렌더링) |
| **@react-three/drei** | 10.7.6 | Three.js 헬퍼 컴포넌트 (카메라, 조명, 로더 등) |

**3D 파일 형식 지원**:
- `.glb` (GLTF Binary): 3D 메쉬 모델
- `.ply` (Polygon File Format): Gaussian Splatting 포인트 클라우드
- `.splat`: Gaussian Splatting 전용 포맷

### 2.4 HTTP 통신 및 유틸리티

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| **Axios** | 1.13.1 | HTTP 클라이언트 (API 통신) |
| **React Icons** | 5.5.0 | 아이콘 컴포넌트 라이브러리 |

**API 통신 설정**:
- Base URL: `process.env.REACT_APP_API_URL` (환경 변수)
- Credentials: `withCredentials: true` (쿠키 기반 인증)

### 2.5 테스팅 프레임워크

| 도구 | 버전 | 용도 |
|------|------|------|
| **@testing-library/react** | 16.3.0 | React 컴포넌트 테스트 |
| **@testing-library/jest-dom** | 6.9.1 | Jest DOM 매처 |
| **@testing-library/user-event** | 13.5.0 | 사용자 이벤트 시뮬레이션 |
| **Jest** | - | React Scripts에 포함된 테스트 러너 |

### 2.6 라우팅 구조

```
/ (HomePage)
├── /signin (LoginPage)
├── /creator/* (CreatorPage)
│   ├── /creator/library (ProductLibrary)
│   ├── /creator/projects (MyProjects)
│   ├── /creator/create (CreateProject)
│   ├── /creator/product/:id (ProductDetail)
│   ├── /creator/project/:id (ProjectDetail)
│   └── /creator/profile (Profile)
└── /brand/* (BrandPage)
    └── /brand/assets (BrandAssets)
```

### 2.7 브라우저 지원

**Production 타겟**:
- 시장 점유율 0.2% 이상 브라우저
- `not dead` (더 이상 업데이트되지 않는 브라우저 제외)
- `not op_mini all` (Opera Mini 제외)

**Development 타겟**:
- Chrome 최신 버전
- Firefox 최신 버전
- Safari 최신 버전

---

## 3. Backend 개발 환경

### 3.1 런타임 및 언어

| 항목 | 버전 | 비고 |
|------|------|------|
| **Node.js** | v22.21.1 | LTS 버전 |
| **npm** | v10.9.4 | 패키지 관리자 |
| **TypeScript** | 5.9.3 | 정적 타입 언어 |
| **Module System** | ES Modules | `"type": "module"` in package.json |

**TypeScript 컴파일러 설정**:
```typescript
{
  "target": "esnext",
  "module": "nodenext",          // Node.js ESM 지원
  "lib": ["esnext"],
  "rootDir": "./src",
  "outDir": "./dist",
  "strict": true,                // 엄격 모드
  "verbatimModuleSyntax": true,  // 명시적 import/export
  "sourceMap": true,             // 디버깅용 소스맵
  "declaration": true            // .d.ts 타입 선언 파일 생성
}
```

### 3.2 웹 프레임워크

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| **Express** | 5.1.0 | 웹 애플리케이션 프레임워크 |
| **cors** | 2.8.5 | CORS 미들웨어 |
| **cookie-parser** | 1.4.7 | 쿠키 파싱 미들웨어 |
| **express-session** | 1.18.2 | 세션 관리 (Google OAuth용) |

**Express 서버 구성**:
- Port: `process.env.PORT` (기본값: 3000)
- CORS Origin: `process.env.CLIENT_URL` (기본값: `http://localhost:3001`)
- Credentials: `true` (쿠키 허용)
- Body Parser: `express.json()` (JSON 요청 파싱)

### 3.3 데이터베이스 및 ORM

| 구성 요소 | 버전 | 역할 |
|----------|------|------|
| **PostgreSQL** | - | 관계형 데이터베이스 |
| **Prisma Client** | 6.18.0 | ORM 클라이언트 |
| **Prisma CLI** | 6.18.0 | 마이그레이션 및 스키마 관리 |

**Prisma 설정**:
```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}
```

**데이터 모델** (총 22개 테이블):
1. 사용자 관련: `Creator`, `Platform_User`, `Admin`, `Content_Partner`
2. 브랜드: `Brand`, `Subscription`, `Subscription_Plan`
3. 제품: `Product`, `Product_Asset`, `Product_Purchase`
4. 자산: `Master_Asset`, `Master_Asset_version`, `Master_Asset_History`, `BrandVault_Library`, `Brand_Library_Access`
5. 프로젝트: `Project`
6. 계약/문의: `AD_Contract`, `Inquiry`, `Message`
7. 리포트: `Analytics_Report`, `Report_purchase`
8. 로그: `Data_Usage_Log`, `Asset_Request`

**성능 인덱스**:
```prisma
// Product 테이블
@@index([view_count, product_id])      // 조회수 기준 정렬
@@index([download_count, product_id])  // 인기순 정렬
@@index([created_at, product_id])      // 최신순 정렬

// Brand 테이블
@@index([asset_count])                 // 자산 개수 정렬
```

### 3.4 인증 및 보안

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| **jsonwebtoken** | 9.0.2 | JWT 토큰 생성/검증 |
| **bcrypt** | 6.0.0 | 비밀번호 해싱 (bcrypt 알고리즘) |
| **passport** | 0.7.0 | 인증 미들웨어 프레임워크 |
| **passport-google-oauth20** | 2.0.0 | Google OAuth 2.0 전략 |

**인증 메커니즘**:
1. **JWT 인증**:
   - Secret: `process.env.JWT_SECRET`
   - 저장 방식: HttpOnly 쿠키 (`jwt` 쿠키명)
   - 만료 시간: 1시간 (`expiresIn: '1h'`)

2. **Google OAuth 2.0**:
   - Client ID: `process.env.GOOGLE_CLIENT_ID`
   - Client Secret: `process.env.GOOGLE_CLIENT_SECRET`
   - Callback URL: `process.env.GOOGLE_CALLBACK_URL`
   - 전략: `passport-google-oauth20`

3. **세션 관리**:
   - Secret: `process.env.SESSION_SECRET` (폴백: `process.env.JWT_SECRET`)
   - Secure: `process.env.NODE_ENV === 'production'` (HTTPS 전용)
   - MaxAge: 24시간

### 3.5 파일 처리

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| **multer** | 2.0.2 | 멀티파트 폼 데이터 파싱 (파일 업로드) |
| **sharp** | 0.34.5 | 이미지 리사이징 및 최적화 |
| **uuid** | 13.0.0 | 고유 파일명 생성 |

**파일 저장 경로**:
- 업로드: `/uploads` (서버 로컬 또는 GCS)
- Splat 파일: `/splats` (3D 모델 저장)
- 임시 파일: `/temp/{uuid}` (작업 후 삭제)

### 3.6 클라우드 스토리지

| 서비스 | SDK 버전 | 용도 |
|--------|----------|------|
| **Google Cloud Storage** | 7.17.2 | 파일 영구 저장소 |

**GCS 설정**:
- Bucket Name: `process.env.GCS_BUCKET_NAME`
- 인증: Google Cloud 서비스 계정 키 (자동 감지)
- 서명된 URL 만료: 1시간 (Replicate API 접근용)
- 파일 경로 구조:
  - `temp/`: 임시 파일 (마스크, 중간 결과물)
  - `videos/`: 최종 비디오 파일
  - `images/`: 업로드된 이미지

### 3.7 AI/ML 서비스

| 서비스 | SDK 버전 | 모델 |
|--------|----------|------|
| **Replicate** | 1.3.1 | AI 모델 추론 플랫폼 |

**사용 모델**:

1. **Gaussian Splat 변환**:
   - 모델: `firtoz/trellis:e8f6c45206993f297372f5436b90350817bd9b4a0d52d2a76df50c1c8afa2b3c`
   - 입력: 단일 이미지 (Base64 data URI)
   - 출력: GLB 또는 PLY 파일
   - 파라미터:
     - `texture_size: 1024`
     - `mesh_simplify: 0.95`
     - `ss_sampling_steps: 12`
     - `slat_sampling_steps: 12`

2. **비디오 제품 삽입 파이프라인**:

   a. **SAM2** (Segmentation):
   - 모델: `meta/sam-2:fe97b453a6455861e3bac769b441ca1f1086110da7466dbb65cf1eecfd60dc83`
   - 용도: 제품 마스크 생성
   - 파라미터:
     - `points_per_side: 32`
     - `pred_iou_thresh: 0.88`
     - `stability_score_thresh: 0.95`

   b. **Anydoor**:
   - 모델: `ali-vilab/anydoor:542c963129c4661ab53a875b1b9a84b2102ca784cf872ef2752a468721c0eb2a`
   - 용도: 첫 프레임에 제품 삽입
   - 파라미터:
     - `steps: 100` (고품질)
     - `guidance_scale: 7.5`
     - `control_strength: 1.0`

   c. **AnyV2V**:
   - 모델: `tiger-ai-lab/anyv2v:3c7b5bc5bcae13a0c945e6f918bb8409f76cc46102c7c9a208bd1531f82c5684`
   - 용도: 전체 비디오 생성
   - 파라미터:
     - `num_inference_steps: 100`
     - `guidance_scale: 12`
     - `ddim_inversion_steps: 200`

**비디오 처리 최적화**:
- 프레임 리샘플링: 16프레임 이하로 리샘플링 후 처리 (속도 향상)
- 프레임 복원: 처리 후 원본 프레임 수로 복원 (품질 유지)
- FFmpeg 사용: 프레임 추출/합성

### 3.8 실시간 통신

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| **Socket.io** | 4.8.1 | WebSocket 기반 실시간 통신 (미구현, 향후 사용 예정) |

### 3.9 API 문서화

| 도구 | 버전 | 용도 |
|------|------|------|
| **swagger-jsdoc** | 6.2.8 | JSDoc → OpenAPI 3.0 스펙 생성 |
| **swagger-ui-express** | 5.0.1 | Swagger UI 제공 |

**API 문서 엔드포인트**: `/api-docs`

**문서화된 API 그룹**:
- Auth: 인증 (로그인, 회원가입, Google OAuth)
- Products: 제품 CRUD, 검색, 필터링
- Projects: 프로젝트 관리
- Brands: 브랜드 정보
- Splat: 3D 변환
- Files: 파일 업로드/다운로드

### 3.10 테스팅

| 도구 | 버전 | 용도 |
|------|------|------|
| **Jest** | 30.2.0 | 테스트 프레임워크 |
| **ts-jest** | 29.4.5 | TypeScript → Jest 트랜스파일러 |
| **Supertest** | 7.1.4 | HTTP 통합 테스트 |

**Jest 설정**:
```javascript
{
  preset: 'ts-jest/presets/default-esm',
  testEnvironment: 'node',
  extensionsToTreatAsEsm: ['.ts'],
  testTimeout: 10000,                    // 10초
  collectCoverageFrom: ['src/**/*.ts'],  // 커버리지 대상
  setupFilesAfterEnv: ['<rootDir>/src/__tests__/setup.ts']
}
```

**테스트 환경 변수** (`src/__tests__/setup.ts`):
```typescript
process.env.NODE_ENV = 'test';
process.env.JWT_SECRET = 'test-secret-key';
process.env.DATABASE_URL = process.env.TEST_DATABASE_URL;
process.env.REPLICATE_API_TOKEN = 'test-replicate-token';
```

### 3.11 개발 도구

| 도구 | 버전 | 용도 |
|------|------|------|
| **tsx** | 4.20.6 | TypeScript 실행 및 Hot Reload |
| **ts-node** | 10.9.2 | TypeScript 직접 실행 |

**개발 스크립트**:
```json
{
  "dev": "tsx watch src/index.ts",      // Hot reload 개발 서버
  "build": "tsc",                        // TypeScript 컴파일
  "start": "node dist/index.js",         // 프로덕션 실행
  "test": "NODE_ENV=test jest"           // 테스트 실행
}
```

---

## 4. 데이터베이스 구성

### 4.1 데이터베이스 시스템

| 항목 | 상세 |
|------|------|
| **DBMS** | PostgreSQL |
| **ORM** | Prisma 6.18.0 |
| **마이그레이션** | Prisma Migrate |
| **연결 URL** | `process.env.DATABASE_URL` |

### 4.2 주요 테이블 구조

#### 4.2.1 사용자 및 인증
```prisma
model Creator {
  creator_id         Int      @id @default(autoincrement())
  user_name          String
  email              String   @unique
  password           String   // bcrypt 해시 (Google 로그인 시 빈 문자열)
  subscription_type  String   @default("free")  // free, pro, enterprise
  created_at         DateTime @default(now())
}
```

#### 4.2.2 제품
```prisma
model Product {
  product_id     Int       @id @default(autoincrement())
  brand_id       Int
  product_name   String
  category       String
  image_url      String?
  model_3d_url   String?   // GLB/PLY/SPLAT 파일 URL
  view_count     Int       @default(0)
  download_count Int       @default(0)
  like_count     Int       @default(0)
  created_at     DateTime  @default(now())

  @@index([view_count, product_id])
  @@index([download_count, product_id])
  @@index([created_at, product_id])
}
```

#### 4.2.3 프로젝트 (비디오 삽입 작업)
```prisma
model Project {
  project_id    Int      @id @default(autoincrement())
  project_name  String
  creator_id    Int
  status        String   @default("completed")  // pending, processing, completed, failed
  job_id        String?  // 비디오 삽입 작업 ID
  progress      Int      @default(0)  // 0-100
  thumbnail_url String?
  created_at    DateTime @default(now())
}
```

### 4.3 인덱스 전략

성능 최적화를 위한 복합 인덱스:
- `Product`: 조회순(`view_count`), 인기순(`download_count`), 최신순(`created_at`)
- `Brand`: 자산 개수(`asset_count`)

### 4.4 관계 설정

- `Creator` ↔ `Project`: 1:N
- `Creator` ↔ `Product_Purchase`: 1:N
- `Brand` ↔ `Product`: 1:N
- `Product` ↔ `Product_Asset` ↔ `Master_Asset_version`: N:M

---

## 5. 클라우드 인프라

### 5.1 호스팅 플랫폼

| 구성 요소 | 플랫폼 | 배포 방식 |
|----------|--------|-----------|
| **Frontend** | Vercel | Serverless (정적 파일 + Edge Network) |
| **Backend** | 미정 (Render/자체 서버) | Node.js 서버 |
| **Database** | 외부 PostgreSQL | Managed DB (Vercel Postgres/Supabase 등) |

### 5.2 Vercel 배포 설정 (Frontend)

**빌드 설정**:
- Build Command: `cd client && npm install && npm run build`
- Output Directory: `client/build`
- Framework: Other (Create React App)

**환경 변수**:
```bash
REACT_APP_API_URL=https://[backend-url]
```

**배포 트리거**:
- `main` 브랜치 푸시 → Production 배포
- PR 생성 → Preview 배포

### 5.3 Google Cloud Storage

**Bucket 설정**:
- Bucket Name: `process.env.GCS_BUCKET_NAME`
- Region: 자동 선택
- Storage Class: Standard
- Access Control: Uniform (서명된 URL 사용)

**사용 패턴**:
1. 임시 파일 업로드 → 서명된 URL 생성 (1시간 만료)
2. Replicate API에 서명된 URL 전달
3. 처리 완료 후 최종 파일 저장 또는 삭제

### 5.4 외부 API 서비스

| 서비스 | 용도 | 인증 |
|--------|------|------|
| **Replicate** | AI 모델 추론 (3D 변환, 비디오 편집) | API Token (`process.env.REPLICATE_API_TOKEN`) |
| **Google OAuth** | 소셜 로그인 | Client ID/Secret |

---

## 6. 환경 변수 구성

### 6.1 Backend 환경 변수

| 변수명 | 필수 여부 | 용도 | 기본값 |
|--------|----------|------|--------|
| `DATABASE_URL` | **필수** | PostgreSQL 연결 URL | - |
| `JWT_SECRET` | **필수** | JWT 토큰 서명 키 | - |
| `PORT` | 선택 | 서버 포트 | 3000 |
| `NODE_ENV` | 선택 | 환경 구분 | development |
| `CLIENT_URL` | 선택 | CORS 허용 URL | `http://localhost:3001` |
| `SESSION_SECRET` | 선택 | 세션 암호화 키 | `JWT_SECRET` 사용 |
| `GOOGLE_CLIENT_ID` | 선택 | Google OAuth ID | - |
| `GOOGLE_CLIENT_SECRET` | 선택 | Google OAuth Secret | - |
| `GOOGLE_CALLBACK_URL` | 선택 | OAuth 콜백 URL | - |
| `GCS_BUCKET_NAME` | **필수** | GCS 버킷 이름 | - |
| `REPLICATE_API_TOKEN` | **필수** | Replicate API 토큰 | - |
| `REPLICATE_API_BASE_URL` | 선택 | Replicate API URL | `https://api.replicate.com/v1` |

### 6.2 Frontend 환경 변수

| 변수명 | 필수 여부 | 용도 |
|--------|----------|------|
| `REACT_APP_API_URL` | **필수** | Backend API URL |

### 6.3 환경별 설정

**Development** (로컬 개발):
```bash
# Backend
DATABASE_URL=postgresql://user:password@localhost:5432/brandvault
JWT_SECRET=dev-secret-key
CLIENT_URL=http://localhost:3001
GCS_BUCKET_NAME=brandvault-dev
REPLICATE_API_TOKEN=r8_xxx...

# Frontend
REACT_APP_API_URL=http://localhost:3000
```

**Production** (배포):
```bash
# Backend
DATABASE_URL=postgresql://user:password@prod-host:5432/brandvault_prod
JWT_SECRET=[강력한 랜덤 키]
NODE_ENV=production
CLIENT_URL=https://brand-vault.vercel.app
GCS_BUCKET_NAME=brandvault-production
REPLICATE_API_TOKEN=r8_xxx...

# Frontend
REACT_APP_API_URL=https://[backend-domain]
```

---

## 7. 개발 환경 설정

### 7.1 필수 소프트웨어

| 소프트웨어 | 버전 | 용도 |
|-----------|------|------|
| Node.js | v22.21.1 | JavaScript 런타임 |
| npm | v10.9.4 | 패키지 관리 |
| PostgreSQL | 최신 | 데이터베이스 |
| FFmpeg | 최신 | 비디오 처리 (Homebrew 경로 지원) |

### 7.2 로컬 실행 방법

#### Frontend 실행
```bash
cd client
npm install
PORT=3001 npm start
# → http://localhost:3001
```

#### Backend 실행
```bash
cd server
npm install
npx prisma generate
npx prisma migrate dev
npm run dev
# → http://localhost:3000
# → http://localhost:3000/api-docs (Swagger)
```

### 7.3 FFmpeg 경로 설정

시스템은 다음 경로에서 FFmpeg를 자동 탐색합니다:
1. `/opt/homebrew/bin/ffmpeg` (Apple Silicon Mac)
2. `/usr/local/bin/ffmpeg` (Intel Mac)
3. `ffmpeg` (시스템 PATH)

---

## 8. 운영 환경 배포

### 8.1 Frontend 배포 (Vercel)

**자동 배포 워크플로우**:
1. GitHub `main` 브랜치 푸시
2. Vercel이 자동으로 빌드 시작
3. `cd client && npm install && npm run build` 실행
4. `client/build` 디렉토리를 CDN에 배포
5. 글로벌 Edge Network로 배포 완료

**배포 URL**: https://brand-vault.vercel.app

### 8.2 Backend 배포 옵션

#### 옵션 1: Render 배포
```bash
# Build Command
npm install && npx prisma generate && npm run build

# Start Command
npm start

# Environment
Node.js (v22.21.1)
```

#### 옵션 2: 자체 서버 배포 (PM2)
```bash
# 빌드
cd server
npm install
npx prisma generate
npm run build

# PM2로 프로세스 관리
pm2 start dist/index.js --name brandvault-api
pm2 save
pm2 startup
```

### 8.3 데이터베이스 마이그레이션

```bash
# 개발 환경
npx prisma migrate dev --name migration_name

# 프로덕션 배포
npx prisma migrate deploy
```

---

## 9. API 엔드포인트

### 9.1 인증 (`/api/auth`)
- `POST /api/auth/register` - 회원가입
- `POST /api/auth/login` - 로그인 (JWT 발급)
- `GET /api/auth/google` - Google OAuth 시작
- `GET /api/auth/google/callback` - Google OAuth 콜백

### 9.2 제품 (`/api/products`)
- `GET /api/products` - 제품 목록 (필터링/정렬 지원)
- `GET /api/products/:id` - 제품 상세
- `POST /api/products` - 제품 생성
- `PUT /api/products/:id` - 제품 수정
- `DELETE /api/products/:id` - 제품 삭제

### 9.3 프로젝트 (`/api/projects`)
- `GET /api/projects` - 프로젝트 목록
- `POST /api/projects` - 프로젝트 생성
- `GET /api/projects/:id` - 프로젝트 상세

### 9.4 3D 변환 (`/api/splat`)
- `POST /api/splat/upload` - 이미지 업로드 및 3D 변환 시작
- `GET /api/splat/status/:jobId` - 변환 작업 상태 확인
- `GET /api/splat/download/:jobId` - 변환된 3D 파일 다운로드

### 9.5 비디오 삽입 (`/api/video-insertion`)
- `POST /api/video-insertion/start` - 비디오 삽입 작업 시작
- `GET /api/video-insertion/status/:jobId` - 작업 진행 상태 (SSE)
- `GET /api/video-insertion/download/:jobId` - 완성된 비디오 다운로드

---

## 10. 보안 구성

### 10.1 인증 보안

- **JWT 토큰**: HttpOnly 쿠키로 저장 (XSS 방지)
- **비밀번호**: bcrypt 해싱 (솔트 라운드 자동)
- **CORS**: 특정 도메인만 허용 (`CLIENT_URL`)
- **세션**: `secure: true` (프로덕션 HTTPS 전용)

### 10.2 API 보안

- **인증 미들웨어**: JWT 검증 (`authMiddleware.ts`)
- **HTTPS 강제**: Vercel/Render 자동 지원
- **환경 변수**: 민감 정보는 코드에 하드코딩 금지

---

## 11. 성능 최적화

### 11.1 Frontend
- **Code Splitting**: React Router 기반 페이지별 분리
- **3D 렌더링**: Gaussian Splatting (효율적인 3D 표현)
- **CDN**: Vercel Edge Network (전 세계 분산)

### 11.2 Backend
- **데이터베이스 인덱스**: 조회수, 인기순, 최신순 정렬 최적화
- **비디오 처리**: 16프레임 리샘플링으로 AI 처리 속도 향상
- **파일 스트리밍**: 대용량 파일 처리 시 메모리 효율화

### 11.3 AI 처리
- **프레임 리샘플링**: 최대 16프레임으로 제한 후 복원
- **폴링 간격**: 상태 변경 없을 시 5초 대기 (API 부하 감소)
- **타임아웃**: 최대 15분 대기 (AnyV2V 모델용)

---

## 12. 프로젝트 디렉토리 구조

```
BrandVault_final/
├── client/                          # Frontend (React)
│   ├── public/                      # 정적 파일
│   ├── src/
│   │   ├── api/                     # API 클라이언트 함수
│   │   ├── components/              # 재사용 컴포넌트
│   │   │   ├── SplatUploader/       # 3D 업로드 UI
│   │   │   └── SplatViewer/         # 3D 뷰어
│   │   ├── pages/                   # 페이지 컴포넌트
│   │   │   ├── Creator/             # 크리에이터 페이지
│   │   │   └── Brand/               # 브랜드 페이지
│   │   └── App.tsx                  # 앱 루트
│   └── package.json
│
├── server/                          # Backend (Express)
│   ├── prisma/schema.prisma         # 데이터베이스 스키마
│   ├── src/
│   │   ├── controllers/             # 컨트롤러
│   │   ├── services/                # 비즈니스 로직
│   │   │   ├── splatService.ts      # 3D 변환
│   │   │   └── videoInsertService.ts # AI 비디오 편집
│   │   ├── routes/                  # 라우트 정의
│   │   ├── midwares/                # 미들웨어
│   │   └── config/                  # 설정 (OAuth, Swagger)
│   └── package.json
│
└── 개발_운영_인프라_기술서.md       # 본 문서
```

---

## 13. 주요 기능 구현 상세

### 13.1 3D Gaussian Splat 변환

**워크플로우**:
1. 사용자가 이미지 업로드 (`POST /api/splat/upload`)
2. 서버가 이미지를 Base64 Data URI로 변환
3. Replicate `firtoz/trellis` 모델 호출
4. 모델이 GLB 또는 PLY 파일 생성
5. 파일을 `/splats/{jobId}.glb` 또는 `.ply`로 저장
6. 클라이언트가 `/api/splat/download/:jobId`로 다운로드

**파일**: `server/src/services/splatService.ts`

### 13.2 AI 비디오 제품 삽입

**파이프라인**:
1. 사용자가 제품 이미지 + 타겟 비디오 + 좌표 제공
2. **프레임 리샘플링**: 비디오를 16프레임 이하로 축소
3. **SAM2**: 제품 이미지에서 마스크 생성
4. **타겟 마스크**: Sharp로 원형 마스크 생성
5. **첫 프레임 추출**: FFmpeg로 비디오 첫 프레임 추출
6. **Anydoor**: 첫 프레임에 제품 삽입
7. **AnyV2V**: 편집된 첫 프레임으로 전체 비디오 생성
8. **프레임 복원**: 16프레임 비디오를 원본 프레임 수로 복원
9. GCS에 최종 비디오 업로드
10. SSE로 실시간 진행 상태 전송

**파일**:
- `server/src/services/videoInsertService.ts` (AI 처리)
- `server/src/services/videoInsertJobService.ts` (작업 관리)

---

## 14. 연락처

- **이메일**: hbg1345@gmail.com
- **배포 사이트**: https://brand-vault.vercel.app

---

**문서 끝**

최종 평가를 위한 소스코드 검증 및 실행 테스트 시 참고 부탁드립니다.
