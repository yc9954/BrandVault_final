# BrandVault - 개발/운영 인프라 기술서

## 문서 정보
- **프로젝트명**: BrandVault
- **작성일**: 2025-11-29
- **홈페이지**: https://brand-vault.vercel.app

---

## 1. 프로젝트 개요

BrandVault는 브랜드 자산 관리 및 3D 제품 모델링을 지원하는 풀스택 웹 애플리케이션입니다.
- **아키텍처**: Client-Server 분리형 아키텍처 (SPA + RESTful API)
- **배포 방식**: Frontend (Vercel), Backend (Render)
- **주요 기능**:
  - 3D 제품 모델 업로드 및 뷰어 (Gaussian Splat)
  - 브랜드/크리에이터/광고주 다중 역할 지원
  - 제품 마켓플레이스
  - 비디오 삽입 기능 (Replicate API)
  - 실시간 프로젝트 진행 상태 추적
  - Google OAuth 인증

---

## 2. Frontend 기술 스택

### 2.1 핵심 프레임워크 및 라이브러리

| 구분 | 기술/도구 | 버전 | 용도 |
|------|----------|------|------|
| **런타임** | Node.js | v22.21.1 | JavaScript 런타임 환경 |
| **패키지 매니저** | npm | v10.9.4 | 의존성 관리 |
| **프레임워크** | React | 19.2.0 | UI 라이브러리 |
| **프레임워크** | React DOM | 19.2.0 | React 렌더링 엔진 |
| **빌드 도구** | React Scripts | 5.0.1 | Create React App 빌드 시스템 |
| **언어** | TypeScript | 4.9.5 | 정적 타입 체크 |
| **라우팅** | React Router DOM | 7.9.4 | SPA 라우팅 |
| **HTTP 클라이언트** | Axios | 1.13.1 | API 통신 |

### 2.2 3D 그래픽 및 시각화

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| Three.js | 0.181.0 | 3D 그래픽 렌더링 엔진 |
| @react-three/fiber | 9.4.0 | React용 Three.js 래퍼 |
| @react-three/drei | 10.7.6 | Three.js 헬퍼 컴포넌트 |

### 2.3 UI 및 스타일링

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| React Icons | 5.5.0 | 아이콘 컴포넌트 |
| CSS Modules | - | 컴포넌트별 스타일 격리 |

### 2.4 테스팅

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| @testing-library/react | 16.3.0 | React 컴포넌트 테스트 |
| @testing-library/jest-dom | 6.9.1 | Jest DOM 매처 |
| @testing-library/user-event | 13.5.0 | 사용자 이벤트 시뮬레이션 |
| @types/jest | 27.5.2 | Jest 타입 정의 |

### 2.5 TypeScript 설정

```json
{
  "compilerOptions": {
    "target": "es5",
    "lib": ["dom", "dom.iterable", "esnext"],
    "jsx": "react-jsx",
    "module": "esnext",
    "moduleResolution": "node",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "allowJs": true,
    "isolatedModules": true,
    "noEmit": true
  }
}
```

### 2.6 브라우저 지원

#### 프로덕션 환경
- 시장 점유율 0.2% 이상
- 더 이상 업데이트되지 않는 브라우저 제외
- Opera Mini 제외

#### 개발 환경
- Chrome (최신 버전)
- Firefox (최신 버전)
- Safari (최신 버전)

---

## 3. Backend 기술 스택

### 3.1 핵심 프레임워크 및 런타임

| 구분 | 기술/도구 | 버전 | 용도 |
|------|----------|------|------|
| **런타임** | Node.js | v22.21.1 | JavaScript 서버 런타임 |
| **패키지 매니저** | npm | v10.9.4 | 의존성 관리 |
| **프레임워크** | Express | 5.1.0 | 웹 애플리케이션 프레임워크 |
| **언어** | TypeScript | 5.9.3 | 정적 타입 체크 |
| **모듈 시스템** | ES Modules | - | ECMAScript 모듈 (type: "module") |

### 3.2 데이터베이스 및 ORM

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| Prisma Client | 6.18.0 | ORM 클라이언트 |
| Prisma CLI | 6.18.0 | 데이터베이스 마이그레이션 도구 |
| PostgreSQL | - | 관계형 데이터베이스 (프로덕션) |

### 3.3 인증 및 보안

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| jsonwebtoken | 9.0.2 | JWT 토큰 생성/검증 |
| bcrypt | 6.0.0 | 비밀번호 해싱 |
| passport | 0.7.0 | 인증 미들웨어 |
| passport-google-oauth20 | 2.0.0 | Google OAuth 2.0 전략 |
| express-session | 1.18.2 | 세션 관리 |
| cookie-parser | 1.4.7 | 쿠키 파싱 |

### 3.4 파일 처리 및 스토리지

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| @google-cloud/storage | 7.17.2 | Google Cloud Storage SDK |
| multer | 2.0.2 | 멀티파트 파일 업로드 |
| sharp | 0.34.5 | 이미지 처리 및 리사이징 |
| uuid | 13.0.0 | 고유 ID 생성 |

### 3.5 외부 API 통합

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| replicate | 1.3.1 | Replicate AI API (Splat 변환) |

### 3.6 실시간 통신

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| socket.io | 4.8.1 | WebSocket 기반 실시간 통신 |

### 3.7 API 문서화

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| swagger-jsdoc | 6.2.8 | OpenAPI 스펙 생성 |
| swagger-ui-express | 5.0.1 | Swagger UI 제공 |

### 3.8 미들웨어 및 유틸리티

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| cors | 2.8.5 | CORS 설정 |
| dotenv | 17.2.3 | 환경 변수 관리 |

### 3.9 테스팅

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| Jest | 30.2.0 | 테스트 프레임워크 |
| ts-jest | 29.4.5 | TypeScript용 Jest 트랜스파일러 |
| supertest | 7.1.4 | HTTP 테스트 |
| @types/jest | 30.0.0 | Jest 타입 정의 |

#### Jest 설정
```javascript
{
  preset: 'ts-jest/presets/default-esm',
  testEnvironment: 'node',
  extensionsToTreatAsEsm: ['.ts'],
  testTimeout: 10000,
  collectCoverageFrom: ['src/**/*.ts', '!src/**/*.d.ts', '!src/index.ts']
}
```

### 3.10 개발 도구

| 기술/도구 | 버전 | 용도 |
|----------|------|------|
| tsx | 4.20.6 | TypeScript 실행 및 hot reload |
| ts-node | 10.9.2 | TypeScript 실행 환경 |

### 3.11 TypeScript 설정

```json
{
  "compilerOptions": {
    "target": "esnext",
    "module": "nodenext",
    "lib": ["esnext"],
    "rootDir": "./src",
    "outDir": "./dist",
    "strict": true,
    "esModuleInterop": true,
    "sourceMap": true,
    "declaration": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true
  }
}
```

---

## 4. 데이터베이스

### 4.1 데이터베이스 정보

| 항목 | 상세 |
|------|------|
| **DBMS** | PostgreSQL |
| **ORM** | Prisma 6.18.0 |
| **스키마 관리** | Prisma Migrate |
| **클라이언트** | @prisma/client 6.18.0 |

### 4.2 주요 데이터 모델

프로젝트는 다음과 같은 주요 엔티티를 포함합니다:

1. **사용자 관련**
   - `Creator`: 크리에이터 사용자
   - `Platform_User`: 플랫폼 사용자 (브랜드 소속)
   - `Admin`: 관리자
   - `Content_Partner`: 콘텐츠 파트너

2. **브랜드 및 구독**
   - `Brand`: 브랜드 정보
   - `Subscription`: 구독 정보
   - `Subscription_Plan`: 구독 플랜

3. **제품 및 자산**
   - `Product`: 제품 정보 (3D 모델 포함)
   - `Product_Asset`: 제품-자산 연결
   - `Master_Asset`: 마스터 자산
   - `Master_Asset_version`: 자산 버전 관리
   - `BrandVault_Library`: 자산 라이브러리

4. **프로젝트 및 구매**
   - `Project`: 크리에이터 프로젝트
   - `Product_Purchase`: 제품 구매 내역
   - `Report_purchase`: 리포트 구매

5. **계약 및 문의**
   - `AD_Contract`: 광고 계약
   - `Inquiry`: 문의사항
   - `Message`: 메시지

### 4.3 데이터베이스 인덱스

성능 최적화를 위한 인덱스:
- `Brand.asset_count`: 자산 개수 기준 정렬
- `Product.view_count`: 조회수 기준 정렬
- `Product.download_count`: 인기순 정렬
- `Product.created_at`: 최신순 정렬

---

## 5. 클라우드 인프라 및 서비스

### 5.1 호스팅 및 배포

| 구분 | 서비스 | 용도 | 비고 |
|------|--------|------|------|
| **Frontend** | Vercel | SPA 정적 파일 호스팅 | Serverless 배포 |
| **Backend** | Render/자체 서버 | Node.js API 서버 | 선택 가능 |
| **Database** | 외부 PostgreSQL | 데이터베이스 | Vercel Postgres/Supabase 등 |

### 5.2 스토리지

| 서비스 | 용도 | SDK 버전 |
|--------|------|----------|
| **Google Cloud Storage** | 파일 업로드 (이미지, 3D 모델 등) | 7.17.2 |

**GCS 버킷 구성**:
- 버킷명: 환경 변수 `GCS_BUCKET_NAME`으로 설정
- 인증: Google Cloud 서비스 계정 키 사용

### 5.3 외부 API 서비스

| 서비스 | 용도 | 라이브러리 버전 |
|--------|------|----------------|
| **Replicate AI** | Splat 변환 (2D 이미지 → 3D Gaussian Splat) | 1.3.1 |
| **Google OAuth 2.0** | 소셜 로그인 | passport-google-oauth20 2.0.0 |

---

## 6. 개발 환경 설정

### 6.1 시스템 요구사항

| 항목 | 버전/사양 |
|------|----------|
| **OS** | Linux, macOS, Windows |
| **Node.js** | v22.21.1 이상 |
| **npm** | v10.9.4 이상 |
| **PostgreSQL** | 최신 안정 버전 권장 |

### 6.2 로컬 개발 환경 설정

#### 1) 저장소 클론
```bash
git clone https://github.com/yc9954/BrandVault_final.git
cd BrandVault_final
```

#### 2) Frontend 설정
```bash
cd client
npm install
```

**실행**:
```bash
PORT=3001 npm start
```
- 브라우저: http://localhost:3001
- Hot reload 활성화

#### 3) Backend 설정
```bash
cd server
npm install
```

**Prisma 설정**:
```bash
# Prisma Client 생성
npx prisma generate

# 데이터베이스 마이그레이션
npx prisma migrate dev
```

**실행**:
```bash
# 개발 모드 (hot reload)
npm run dev

# 프로덕션 빌드
npm run build
npm start
```
- 서버: http://localhost:3000
- API 문서: http://localhost:3000/api-docs

### 6.3 환경 변수 설정

#### Frontend (.env)
```bash
REACT_APP_API_URL=http://localhost:3000
```

#### Backend (.env)
```bash
# 데이터베이스
DATABASE_URL=postgresql://user:password@localhost:5432/brandvault

# 인증
JWT_SECRET=your-secret-key-change-in-production
SESSION_SECRET=your-session-secret

# CORS
CLIENT_URL=http://localhost:3001

# Google OAuth (선택)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# Google Cloud Storage
GCS_BUCKET_NAME=your-bucket-name

# Replicate AI (선택)
REPLICATE_API_TOKEN=your-replicate-token

# 서버 설정
PORT=3000
NODE_ENV=development
```

### 6.4 IDE 설정

**권장 IDE**:
- Visual Studio Code

**권장 확장**:
- ESLint
- Prettier
- TypeScript and JavaScript Language Features
- Prisma (Prisma 스키마 하이라이팅)

---

## 7. 운영 환경 설정

### 7.1 Frontend 운영 환경 (Vercel)

#### 빌드 설정
| 항목 | 값 |
|------|-----|
| **Framework Preset** | Other |
| **Root Directory** | `./` |
| **Build Command** | `cd client && npm install && npm run build` |
| **Output Directory** | `client/build` |
| **Install Command** | `cd client && npm install && cd ../server && npm install` |

#### 환경 변수
```bash
REACT_APP_API_URL=https://your-backend-url.com
```

#### 배포 방법
1. GitHub 저장소와 Vercel 연동
2. 자동 배포: `main` 브랜치에 푸시 시 자동 배포
3. Preview 배포: PR 생성 시 자동 프리뷰 생성

### 7.2 Backend 운영 환경

#### Render 배포 (추천)
```bash
# Build Command
npm install && npx prisma generate && npm run build

# Start Command
npm start

# Environment
Node.js (v22.21.1)
```

#### 환경 변수 (프로덕션)
```bash
# 데이터베이스
DATABASE_URL=postgresql://user:password@host:5432/brandvault_prod

# 인증
JWT_SECRET=strong-random-secret-change-me
SESSION_SECRET=strong-session-secret

# CORS
CLIENT_URL=https://brand-vault.vercel.app

# Google OAuth
GOOGLE_CLIENT_ID=production-client-id
GOOGLE_CLIENT_SECRET=production-client-secret
GOOGLE_CALLBACK_URL=https://your-backend.onrender.com/api/auth/google/callback

# Google Cloud Storage
GCS_BUCKET_NAME=brandvault-production

# Replicate AI
REPLICATE_API_TOKEN=production-token

# 서버 설정
PORT=3000
NODE_ENV=production
```

#### 프로덕션 체크리스트
- [ ] 강력한 JWT_SECRET 설정
- [ ] DATABASE_URL을 프로덕션 DB로 설정
- [ ] CORS origin을 프로덕션 도메인으로 제한
- [ ] Google OAuth 콜백 URL 업데이트
- [ ] GCS 버킷 권한 확인
- [ ] 환경 변수 모두 설정 확인
- [ ] Prisma 마이그레이션 실행 (`npx prisma migrate deploy`)

### 7.3 데이터베이스 운영

#### 프로덕션 데이터베이스 호스팅 옵션
1. **Vercel Postgres** (Vercel 사용 시 편리)
2. **Supabase** (무료 티어 제공)
3. **Render PostgreSQL** (Render 배포 시 편리)
4. **AWS RDS** (엔터프라이즈급)
5. **Google Cloud SQL** (GCP 생태계)

#### 마이그레이션
```bash
# 개발 환경
npx prisma migrate dev

# 프로덕션 환경 (배포 시 자동 실행 권장)
npx prisma migrate deploy
```

---

## 8. 배포 방식

### 8.1 Frontend 배포 (Vercel)

#### 자동 배포 워크플로우
```
1. 코드 변경 → GitHub push
2. Vercel이 자동으로 변경 감지
3. 빌드 실행: npm install → npm run build
4. 정적 파일을 Vercel CDN에 배포
5. 도메인에 자동 적용
```

#### 배포 환경
- **Production**: `main` 브랜치
- **Preview**: 모든 PR 및 브랜치
- **CDN**: Vercel Edge Network (전 세계 분산)

#### 롤백
- Vercel 대시보드에서 이전 배포로 즉시 롤백 가능

### 8.2 Backend 배포

#### Option 1: Render 배포
```
1. GitHub 저장소 연동
2. 환경 변수 설정
3. 자동 배포: main 브랜치 푸시 시
4. 헬스 체크: /api/health (구현 권장)
```

#### Option 2: 자체 서버 배포
```bash
# 1. 서버에 코드 배포
git pull origin main

# 2. 의존성 설치
cd server && npm install

# 3. Prisma 클라이언트 생성
npx prisma generate

# 4. 데이터베이스 마이그레이션
npx prisma migrate deploy

# 5. 빌드
npm run build

# 6. PM2로 프로세스 관리 (권장)
pm2 start dist/index.js --name brandvault-api
pm2 save
pm2 startup
```

#### 프로세스 관리 (PM2)
```bash
# 설치
npm install -g pm2

# 시작
pm2 start dist/index.js --name brandvault-api

# 모니터링
pm2 monit

# 로그 확인
pm2 logs brandvault-api

# 재시작
pm2 restart brandvault-api

# 중지
pm2 stop brandvault-api
```

### 8.3 CI/CD 파이프라인

현재 프로젝트는 GitHub + Vercel의 자동 배포를 사용합니다.

**향후 권장사항** (GitHub Actions 예시):
```yaml
# .github/workflows/ci.yml
name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'

      # Backend 테스트
      - name: Test Backend
        run: |
          cd server
          npm install
          npm run test

      # Frontend 테스트
      - name: Test Frontend
        run: |
          cd client
          npm install
          npm run test
```

---

## 9. 모니터링 및 로깅

### 9.1 애플리케이션 로그

#### Backend
- Console 로그 (`console.log`, `console.error`)
- Render 대시보드에서 실시간 로그 확인
- PM2 사용 시: `pm2 logs`

#### Frontend
- 브라우저 콘솔
- Vercel Functions 로그 (서버리스 함수 사용 시)

### 9.2 권장 모니터링 도구

| 도구 | 용도 |
|------|------|
| **Sentry** | 에러 추적 및 모니터링 |
| **LogRocket** | 프론트엔드 세션 리플레이 |
| **PM2 Monitoring** | 백엔드 프로세스 모니터링 |
| **Vercel Analytics** | 프론트엔드 성능 분석 |

---

## 10. 보안 설정

### 10.1 인증 및 권한

- **JWT 토큰**: HttpOnly 쿠키로 저장 (XSS 방지)
- **비밀번호 해싱**: bcrypt (솔트 라운드 자동)
- **Google OAuth**: passport-google-oauth20 사용
- **CORS**: 특정 도메인만 허용 (`CLIENT_URL`)

### 10.2 보안 체크리스트

- [ ] JWT_SECRET은 최소 32자 이상 랜덤 문자열
- [ ] 프로덕션에서 `NODE_ENV=production` 설정
- [ ] HTTPS 사용 (Vercel 자동, Render 지원)
- [ ] 환경 변수에 민감 정보 저장, 코드에 하드코딩 금지
- [ ] CORS origin을 프로덕션 도메인으로 제한
- [ ] 쿠키 `secure: true` (프로덕션)
- [ ] Google Cloud Storage 버킷 권한 최소화

---

## 11. 테스트 환경

### 11.1 Backend 테스트

```bash
# 단위 테스트 실행
npm run test

# Watch 모드
npm run test:watch

# 커버리지 리포트
npm run test:coverage
```

**테스트 설정**:
- 프레임워크: Jest 30.2.0
- 프리셋: ts-jest (ESM)
- HTTP 테스트: Supertest 7.1.4
- 타임아웃: 10초
- 환경: `NODE_ENV=test`

### 11.2 Frontend 테스트

```bash
cd client
npm run test
```

**테스트 도구**:
- @testing-library/react 16.3.0
- Jest (React Scripts 포함)
- user-event 시뮬레이션

---

## 12. 프로젝트 구조

### 12.1 디렉토리 구조

```
BrandVault_final/
├── client/                 # Frontend (React)
│   ├── public/            # 정적 파일
│   ├── src/
│   │   ├── api/           # API 클라이언트
│   │   ├── assets/        # 이미지, 폰트 등
│   │   ├── components/    # 재사용 컴포넌트
│   │   │   ├── BrandHeader/
│   │   │   ├── Button/
│   │   │   ├── Header/
│   │   │   ├── Layout/
│   │   │   ├── SplatUploader/
│   │   │   ├── SplatViewer/
│   │   │   └── common/
│   │   ├── hooks/         # 커스텀 훅
│   │   ├── pages/         # 페이지 컴포넌트
│   │   │   ├── Advertiser/
│   │   │   ├── Brand/
│   │   │   ├── Creator/
│   │   │   ├── HomePage.tsx
│   │   │   └── LoginPage.tsx
│   │   ├── types.ts       # TypeScript 타입 정의
│   │   ├── App.tsx        # 앱 루트
│   │   └── index.tsx      # 엔트리 포인트
│   ├── package.json
│   └── tsconfig.json
│
├── server/                # Backend (Express + TypeScript)
│   ├── prisma/
│   │   └── schema.prisma  # Prisma 스키마
│   ├── src/
│   │   ├── __tests__/     # 테스트 파일
│   │   ├── config/        # 설정 (Passport, Swagger)
│   │   ├── controllers/   # 컨트롤러 (비즈니스 로직)
│   │   ├── midwares/      # 미들웨어 (인증 등)
│   │   ├── routes/        # 라우트 정의
│   │   ├── services/      # 서비스 레이어
│   │   ├── types/         # TypeScript 타입
│   │   ├── utils/         # 유틸리티 함수
│   │   ├── app.ts         # Express 앱 설정
│   │   ├── index.ts       # 서버 엔트리 포인트
│   │   ├── db.ts          # Prisma 클라이언트
│   │   └── gcs.ts         # Google Cloud Storage 설정
│   ├── temp/              # 임시 파일 (업로드 등)
│   ├── package.json
│   ├── tsconfig.json
│   └── jest.config.js
│
├── .gitignore
├── README.md
├── ENV_VARS.md            # 환경 변수 가이드
├── VERCEL_DEPLOY.md       # Vercel 배포 가이드
├── TROUBLESHOOTING.md
└── api_spec.txt           # API 명세
```

### 12.2 API 라우트 구조

| 경로 | 설명 |
|------|------|
| `/api/auth` | 인증 (로그인, 회원가입, Google OAuth) |
| `/api/products` | 제품 CRUD, 검색, 필터링 |
| `/api/projects` | 크리에이터 프로젝트 관리 |
| `/api/brands` | 브랜드 정보 관리 |
| `/api/splat` | 3D Splat 업로드 및 변환 |
| `/api/file` | 파일 업로드/다운로드 |
| `/api/video-insertion` | 비디오 삽입 기능 |
| `/api-docs` | Swagger API 문서 |

---

## 13. 성능 최적화

### 13.1 Frontend 최적화

- **Code Splitting**: React Router DOM의 lazy loading
- **이미지 최적화**: Sharp 라이브러리로 서버에서 리사이징
- **CDN**: Vercel Edge Network 사용
- **캐싱**: 브라우저 캐시 활용

### 13.2 Backend 최적화

- **데이터베이스 인덱스**: view_count, download_count, created_at 등
- **Prisma 쿼리 최적화**: Select, Include 최소화
- **파일 스트리밍**: 대용량 파일 처리 시
- **Connection Pooling**: Prisma의 자동 연결 풀

### 13.3 3D 렌더링 최적화

- **Gaussian Splat**: 효율적인 3D 렌더링 기법
- **Three.js 최적화**: LOD (Level of Detail) 고려
- **Lazy Loading**: 필요한 모델만 로드

---

## 14. 트러블슈팅

### 14.1 일반적인 문제

#### 1) "Cannot find module" 오류
```bash
# 의존성 재설치
rm -rf node_modules package-lock.json
npm install
```

#### 2) Prisma 관련 오류
```bash
# Prisma 클라이언트 재생성
npx prisma generate

# 마이그레이션 리셋 (개발 환경만!)
npx prisma migrate reset
```

#### 3) CORS 오류
- `CLIENT_URL` 환경 변수가 프론트엔드 URL과 일치하는지 확인
- 브라우저 개발자 도구에서 요청 헤더 확인

#### 4) Google OAuth 실패
- Google Cloud Console에서 Redirect URI 확인
- `GOOGLE_CALLBACK_URL`이 정확한지 확인

자세한 내용은 `TROUBLESHOOTING.md` 참조

---

## 15. 추가 문서

| 문서 | 설명 |
|------|------|
| `README.md` | 프로젝트 개요 및 빠른 시작 |
| `ENV_VARS.md` | 환경 변수 상세 가이드 |
| `VERCEL_DEPLOY.md` | Vercel 배포 가이드 |
| `TROUBLESHOOTING.md` | 문제 해결 가이드 |
| `api_spec.txt` | API 명세 |

---

## 16. 연락처 및 지원

- **이메일**: hbg1345@gmail.com
- **홈페이지**: https://brand-vault.vercel.app
- **저장소**: GitHub (비공개 시 이메일 문의)

---

## 17. 버전 히스토리

| 버전 | 날짜 | 변경사항 |
|------|------|----------|
| 1.0.0 | 2025-11-29 | 초기 버전 - 개발/운영 인프라 기술서 작성 |

---

## 부록: 빠른 참조

### 로컬 개발 시작
```bash
# Frontend
cd client && PORT=3001 npm start

# Backend
cd server && npm run dev
```

### 프로덕션 빌드
```bash
# Frontend
cd client && npm run build

# Backend
cd server && npm run build && npm start
```

### 테스트 실행
```bash
# Frontend
cd client && npm test

# Backend
cd server && npm run test
```

### 데이터베이스 작업
```bash
# 마이그레이션 생성
npx prisma migrate dev --name migration_name

# 프로덕션 마이그레이션 적용
npx prisma migrate deploy

# Prisma Studio (GUI)
npx prisma studio
```

---

**문서 끝**
